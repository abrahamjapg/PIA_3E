# -MODIFICACIONES NECESARIAS
# -MENU INTERACTIVO FUENTE
import shodanAPI_module

def menu_principal():
    while True:
        print("\nOpciones:")
        print("1. Obtener información con Shodan")
        print("2. abuseapi")
        print("3. dificil")
        print("4. facildiferente1")
        print("5. facildiferente2")
        print("6. Salir")

        opcion = input("Selecciona una opción (1, 2 3, 4, 5, o 6): ")

        if opcion == '1':
            if __name__ == "__main__":
                shodanAPI_module
                    
        elif opcion == '2':
            while True:
                try:
                    
                    else:
                        
                            break
        elif opcion == '3':
            while True:
                try:

        elif opcion == '4':
            while True:
                try:

        elif opcion == '5':
            while True:
                try:
        
        
        elif opcion == '6':
            print("Saliendo del programa...")
            break

        else:
            print("Opción no válida. Por favor, selecciona 1, 2 o 3.")
